apiVersion: rbac.authorization.k8s.io/v1
kind: Role 
metadata:
  namespace: roboshop
  name: roboshop-trainee
rules:
- apiGroup: [""] # "" indiactes the core API group 
  resources: ["pods"]
  verbs: ["get", "watch", "list"]